appId: net.psd401.eoc
name: Issue 32 - verify the registered iOS event URL opened the exact drill room
tags: [issue-32, synthetic-only, ios, event-room, deep-link]
---
- runFlow:
    file: shared/assert-synthetic-context.yaml
- runFlow:
    file: shared/assert-active-drill-room.yaml
    env:
      EVENT_ROUTE_EVIDENCE: ${EVENT_ROUTE_EVIDENCE}
      EVENT_TYPE_NAME: ${EVENT_TYPE_NAME}
      FACILITY_CODE: ${FACILITY_CODE}
      FACILITY_NAME: ${FACILITY_NAME}
