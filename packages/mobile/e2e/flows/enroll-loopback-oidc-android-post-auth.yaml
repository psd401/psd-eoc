appId: net.psd401.eoc
name: Issue 32 - verify authenticated Android loopback enrollment
tags: [issue-32, synthetic-only, android, oidc, post-auth]
---
- runFlow:
    file: shared/assert-synthetic-context.yaml
- runFlow:
    file: shared/retry-loopback-if-offline.yaml
- runFlow:
    file: shared/assert-loopback-enrollment.yaml
    env:
      EVENT_ID: ${EVENT_ID}
      EVENT_TYPE_NAME: ${EVENT_TYPE_NAME}
      FACILITY_CODE: ${FACILITY_CODE}
      FACILITY_NAME: ${FACILITY_NAME}
