import removeStartFlowPlaywrightDatabase from '../../start/test/playwright.global-teardown';

/** Removes the exact marker-owned synthetic database created for this run. */
export default removeStartFlowPlaywrightDatabase;
