# Operational readiness register

<!-- psd-eoc:readiness-register -->

This is the repository's only current source for deployment, DNS, identity,
monitoring, provider, and mobile-distribution readiness. Durable runbooks link
here instead of copying time-sensitive claims. Update an entry only from dated,
retained evidence; do not infer live state from source code or synthesis.

Allowed labels:

- `mocked`: synthetic, fail-closed behavior only;
- `configured-unverified`: configuration or a partial provider boundary is
  proved, but the complete path is not;
- `live-verified`: the stated boundary was observed live on the evidence date;
- `blocked`: a required boundary is unavailable or deliberately disabled.

Last consolidated from retained evidence: **2026-09-08**.

Client-observed failures are reported to `POST /api/diagnostics` and logged as
`client-diagnostic`. This exists because nothing the server logs can explain a
request it never received: an emergency drill ran with the phone reporting an
unavailable timeline while the server recorded nothing at all. The reporter
shipped inert in 1.0.9 -- the client was constructed without it -- and is
connected from 1.0.10, so no client diagnostic before that build exists.

## Status summary

| Boundary                             | Status                  |
| ------------------------------------ | ----------------------- |
| Web application, Aurora, and DNS/TLS | `live-verified`         |
| CloudWatch monitoring                | `configured-unverified` |
| Google OIDC                          | `configured-unverified` |
| Google Groups access membership      | `live-verified`         |
| EAS Build                            | `live-verified`         |
| EAS Update                           | `blocked`               |
| EAS Submit                           | `live-verified`         |
| TestFlight device installation       | `live-verified`         |
| Google Play device installation      | `configured-unverified` |
| Public mobile privacy policy         | `live-verified`         |
| Play app content and store record    | `configured-unverified` |
| Expo Push notification handoff       | `live-verified`         |
| Direct APNs notification handoff     | `mocked`                |
| Direct FCM notification handoff      | `mocked`                |
| Amazon SES notification handoff      | `live-verified`         |
| AWS End User Messaging SMS           | `live-verified`         |
| Event media and record exports       | `blocked`               |
| GuardDuty Malware Protection for S3  | `configured-unverified` |
| OpenStreetMap raster tiles           | `configured-unverified` |

## Evidence by boundary

Each entry states the boundary that the evidence proves and nothing beyond it.

### Web application, Aurora, and DNS/TLS

Status: `live-verified`

On 2026-08-21 the App Runner service reported `RUNNING`; the public custom domain served the application and `/api/health`; the service used private Aurora through its VPC connector. This does not prove notification delivery.

### CloudWatch monitoring

Status: `configured-unverified`

On 2026-08-28 all 25 application alarms were read back with `ActionsEnabled: true`. Twenty-four were `OK`; the App Runner 5xx alarm was `ALARM` from three 5xx responses at 02:42 UTC during this deployment's application cutover, after which the health route returned 200 on three consecutive anonymous requests. Enabling the email worker created its worker-health and provider-incomplete alarms. Publisher-dependent activation, roster, provider-latency, metrics-collector, and canary alarms remain source-defined only. No alarm has been driven to `ALARM` by a forced synthetic failure. On 2026-09-02 a direct `cdk deploy` of merged source `1f2e0f761996de1417de5da755c369e51efeabb0` reached `UPDATE_COMPLETE` after a 610-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-02T13:08:35-07:00, and the configured production origin's `/api/health` returned HTTP 200. That source carries merged `#442`: two metric filters on the bootstrap log group the scheduled membership task writes to, and two alarms on them, `psd-eoc-roster-membership-sync-failed` (the roster leg's `roster-membership-sync-failed` line, paged to the operations topic) and `psd-eoc-access-membership-sync-failed` (the sign-in leg's failed-closed line, paged to the critical topic), each summing over one two-hour period so an `OK` means a scheduled run completed without a failure line. A read-back immediately after the deploy listed 28 `psd-eoc-` alarms, all with `ActionsEnabled: true`; both new alarms were `OK`, both metric filters were present on the bootstrap log group with the expected patterns, twenty-six alarms were `OK`, and two were in `ALARM`: `psd-eoc-email-dlq-depth` and `psd-eoc-push-dlq-depth`, which predate this deploy and mean each dead-letter queue holds at least one message; those are open for the product owner. On 2026-09-08 a direct `cdk deploy` of merged source `1f3f41c5b002ae714a94cce25f649e3b209e14b5` reached `UPDATE_COMPLETE` at 05:10:51Z after a 732-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-07T22:16:17-07:00, and the configured production origin's `/api/health` returned HTTP 200. That source carries merged `#472`, which reworks alarm routing and thresholds after a fortnight in which the `psd-eoc-` alarms produced 175 `ALARM` transitions, each mailed and texted twice because every alarm published its `OK` transition to its own paging topic. A read-back immediately after the deploy listed 33 `psd-eoc-` alarms, all with `ActionsEnabled: true`. Every one of the 32 alarms that carries an `OK` action routes it to the new `psd-eoc-alarm-recoveries` topic, and that topic has exactly one subscription, protocol `lambda` (the alarm mailer): a recovery is mailed and no longer paged. `psd-eoc-aurora-acu-utilization` is absent and `psd-eoc-aurora-capacity-pinned` is present in its place, reading `ServerlessDatabaseCapacity` in ACU rather than `ACUUtilization`, which on this cluster's 0.5-to-1.0 range has only two attainable values and fired on 2026-09-02 with three database connections and no load; cluster capacity was not changed. `psd-eoc-apprunner-5xx-burst` is present alongside `psd-eoc-apprunner-5xx`. The four channel worker services on `psd-eoc-bootstrap` each report 1 desired, 1 running, 0 pending. Every SMS stack parameter carried forward unchanged, `EnableAwsEumSmsWorker` included; `cdk diff` before the deploy showed no `Parameters` section and no parameter override was passed. Thirty-two alarms were `OK` and one was in `ALARM`: `psd-eoc-sms-worker-message-failures`, since 05:08:39Z, from a single datapoint at 05:07:00Z. Its cause is three `LIVE_PROVIDER_DISABLED` failures logged at 05:07:05Z, 05:09:05Z and 05:11:05Z by the SMS worker task started at 04:57:39Z, which ran the preceding deployment of `8fb9f51`; this deployment's SMS worker did not start until 05:13:36Z, so the failures predate it and are not attributable to the alarm changes. That same earlier task did record one `sms-worker-message-completed` with `count: 1` at 05:05:05Z. Whether live SMS send authorization is now correct is open and belongs to merged `#471`, not to this row. Under the new worker the SMS work queue and its dead-letter queue both read 0 messages, and heartbeats were observed at 05:13:36Z and 05:18:37Z -- five minutes apart, the new cadence the fifteen-minute `psd-eoc-sms-worker-health` window requires. No alarm in this deployment has been driven to `ALARM` by a forced synthetic failure, and the reduction in notification volume is a claim about configuration that only a future fortnight of alarm history can verify.

### Google OIDC

Status: `configured-unverified`

On 2026-09-25 a defect was found and fixed that refused every sign-in, Google included, by any account limited to specific facilities: `authorizeSignIn` always handed session issuance a district-wide scope, and the session store refuses a scope that differs from the stored one with "Authoritative facility scope could not be confirmed." The fix (merge `689fb6d7`, deployed the same day) returns the account's stored scope. It surfaced through the app-store review sign-in, which shares the same session issuance; no Google sign-in by a facility-limited account has been observed since. The live sign-in route redirected to Google with the registered callback on 2026-08-21. A completed district sign-in/session result is not retained.

### Google Groups access membership

Status: `live-verified`

The deployed scheduled job read the provider and published a complete fresh access snapshot on 2026-08-21/22 using aggregate, staff-only evidence. On 2026-09-02 a direct `cdk deploy` of merged source `63b9ceb19b295dacd3eea2b312a43bd00648bddd` reached `UPDATE_COMPLETE` after a 949-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-01T19:53:25-07:00, the configured production origin's `/api/health` returned HTTP 200, and the Expo push worker is `ACTIVE` on task definition revision 46 at desired/running/pending 1/1/0 with rollout `COMPLETED`. That source carries merged `#432`: the access-group create and edit forms now post a granted role (staff by default, administrator on request) that the route had always required, so the forms no longer submit a request the route refuses, and the scheduled sync evaluates others-purpose Google sources alongside building sources. That combined evaluation is a known hazard: one roster group with a non-staff member, a nested group, or more people than the evaluator accepts fails the whole scheduled run, and sign-in membership fails closed 24 hours after its last fresh publication, so no others-purpose Google group is registered in production until `#433`, which evaluates and publishes roster groups in their own scoped run, is deployed. Later on 2026-09-02 a direct `cdk deploy` of merged source `7dc265811fe083fe96e67d7a4a20b2700fb9523a` reached `UPDATE_COMPLETE` after a 725-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-01T20:38:15-07:00, the configured production origin's `/api/health` returned HTTP 200, and the Expo push, email, and email-callback workers are `ACTIVE` at desired/running/pending 1/1/0 with rollout `COMPLETED`, the Expo push worker on task definition revision 47. That source carries merged `#433`: the scheduled sync evaluates and publishes the sign-in groups and the roster groups as two scoped runs under distinct idempotency keys, a roster failure is logged as `roster-membership-sync-failed` and leaves the sign-in publication standing, and each run record names its scope through migration `0044_access_snapshot_scope` so a redelivered sign-in run is never reported as superseded by the roster run of the same tick. A read-only bootstrap probe after the deploy read `drizzle.__drizzle_migrations` with its newest row at `created_at` 1788318520403, the `0044` journal entry, the live `access_membership_snapshots.scope` column (`text`, not null, default `'access'`) with its `access_membership_snapshots_scope_valid` CHECK, and 129 snapshot rows all labeled `access` with the latest captured at 2026-09-02T02:01:00Z, the last combined run before the deploy. The combined-evaluation hazard recorded above is closed by this deploy, and an others-purpose Google group may now be registered. No scoped run has happened yet; the next scheduled run's `scope` summary lines, or a `roster-membership-sync-failed` line, are the observation that remains. That observation is now retained: the scheduled run at 2026-09-02T04:01:03Z on source `7dc265811fe083fe96e67d7a4a20b2700fb9523a` logged `access-membership-sync-complete` with `scope: access`, snapshot version 130, one active sign-in group, five evaluated memberships, and publication `created`, and logged no roster line at all, which is the roster leg skipping because no roster Google group is configured; the task emitted no failure line. On 2026-09-02 a direct `cdk deploy` of merged source `433f9f2a921d4c4c999842ffa95ea4e5271aa8a0` reached `UPDATE_COMPLETE` after a 577-second deployment on its second attempt (the first attempt's image push failed after the operator's SSO session expired mid-push and changed nothing); the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-02T10:29:45-07:00, the configured production origin's `/api/health` returned HTTP 200, the App Runner runtime configuration now carries `GOOGLE_ROSTER_CONFIG`, and the Expo push, email, and email-callback workers are `ACTIVE` at desired/running/pending 1/1/0 with rollout `COMPLETED` on task definition revisions 48, 45, and 45. That source carries merged `#437`: a Google group is registered on the Access and Facilities pages by its address alone, and the server resolves its Google Group ID through Cloud Identity under the same identity rules the scheduled sync applies, refusing before anything is saved when Google cannot resolve it; the administrator role is required before that lookup, an access-group edit that keeps its address never contacts Google, and addresses are normalized at the route boundary. Immediately after the deploy the product owner's district staff Google group was registered on the Access page as a staff sign-in group named "PSD EOC district staff", and the page shows the ID Google resolved for it, `<group-id>`; an on-demand run of the access-membership task (task definition revision 58, a pinned manual idempotency key) then logged `access-membership-sync-complete` with `scope: access`, snapshot version 137, two active sign-in groups, seven evaluated memberships, and publication `created`, so that group's members can sign in from that moment. Registering the same group as an others roster source was refused with "That group source is already configured": `group_sources_google_group_id_uq` allows one source per Google Group ID across every purpose, so one Google group cannot yet serve as both a sign-in group and a notification audience; that decision is open with the product owner. Later on 2026-09-02 a direct `cdk deploy` of merged source `a6af0739c654e1f0bc20620151f05e35740d5866` reached `UPDATE_COMPLETE` after a 637-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-02T10:56:50-07:00, the configured production origin's `/api/health` returned HTTP 200, and the Expo push, email, and email-callback workers are `ACTIVE` at desired/running/pending 1/1/0 with rollout `COMPLETED` on task definition revisions 49, 46, and 46. That source carries merged `#438`: at every sign-in the server confirms the person's membership in each active sign-in group with Cloud Identity, after re-resolving each group by its address and requiring the recorded ID, writes a confirmed membership stamped at that instant and removes a denied one under the administrator-availability lock without ever removing the last reachable administrator, and decides access from the fresher of the group's scheduled read and the person's own row; when Google cannot be asked the stored membership decides, bounded by its freshness, exactly as before. A change to a sign-in group therefore takes effect at the person's next sign-in rather than at the next scheduled run. Later still on 2026-09-02 a direct `cdk deploy` of merged source `df6a7642f24af667e4251922ab8fb0b6bf70f2b3` reached `UPDATE_COMPLETE` after a 779-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-02T11:23:31-07:00, the configured production origin's `/api/health` returned HTTP 200, and the Expo push, email, and email-callback workers are `ACTIVE` at desired/running/pending 1/1/0 with rollout `COMPLETED` on task definition revisions 50, 47, and 47. That source carries merged `#440`: one Google group may back one source per purpose, so the district staff group can gate sign-in and be the every-event audience at once, while a building source and an others source still may not share a group. A read-only bootstrap probe after the deploy read `drizzle.__drizzle_migrations` with its newest row at `created_at` 1788371050917, the `0045` journal entry, and the live unique constraints on `group_sources`: `group_sources_google_group_id_purpose_uq` on (`google_group_id`, `purpose`) with the old single-column constraint gone. The district staff Google group was then registered on the Facilities page as an others source named "PSD EOC district staff", showing the same resolved ID `<group-id>` as its sign-in twin, and an on-demand run of the access-membership task (task definition revision 60) logged the first `scope: roster` summary this deployment has produced: snapshot version 140, one roster group, three evaluated memberships, publication `created`, alongside the sign-in scope's version 139 with two groups and seven memberships. The product owner then published the roster from the Facilities page and the page confirmed "A new roster snapshot was published. Activations now reach the saved people.", so the district staff group is the every-event audience from that snapshot on; the sign-in side of the same group had been live since the earlier registration.

### EAS Build

Status: `live-verified`

On 2026-09-25 EAS finished production/store artifacts for embedded-only 1.0.17 from merge `4b6b828e`: iOS build 32 (`a724c5ed-4212-48ce-9ce1-bea44a01171d`) and Android version code 24 (`273bc4c9-4705-46ae-86d6-d19f1bedb4a7`). That commit adds the app-store review sign-in (a code-based sign-in for the one review account, off unless the deployment supplies `AppReviewSignInSha256`) and lets a device with no screen lock finish enrolling instead of discarding the new session. Prior evidence: On 2026-09-22 EAS finished production/store artifacts for embedded-only 1.0.16 from merged commit `444e0ec74`: iOS build 31 (`[redacted]`) and Android version code 23 (`[redacted]`). Both were started from the branch commit `7881c36a` whose tree is identical to the merge, so the EAS source field reads `7881c36`. That commit ends an Android-only dead end in Google sign-in: the OS can deliver `psdeoc://auth/callback` to the app at the same moment `expo-web-browser` is waiting for it, and when the OS won that race the client discarded the authorization code, erased the PKCE verifier, and reported that sign-in was cancelled. iOS never reached it because `ASWebAuthenticationSession` captures the redirect first. Prior evidence: On 2026-09-08 EAS finished production/store artifacts for embedded-only 1.0.14 from merged commit `e288104675249295edec646102b1516bc45e49f1`: iOS build 26 (`[redacted]`) and Android version code 17 (`[redacted]`). That commit carries the contract every installed 1.0.13 build lacked: the activation preview's channel plan names `integrationId` instead of the retired `integrationStatus`, so 1.0.13 rejected every preview from the server deployed at `ebf5bca` with "PSD EOC received an invalid authenticated response" and could not start a drill from a phone; it also moves expo to 57.0.21 and expo-router to 57.0.20. Prior evidence: on 2026-09-05 EAS finished 1.0.13 from merged commit `18ce5eef40ee36c587ebac709301c867309d235c`: iOS build 25 (`[redacted]`) and Android version code 16 (`[redacted]`). Prior evidence: on 2026-09-04 EAS finished 1.0.12 from merged commit `769a2840`: iOS build 24 (`[redacted]`) and Android version code 15 (`[redacted]`). Prior evidence: on 2026-09-01 EAS finished production/store artifacts for embedded-only 1.0.10 from merged commit `718eccda89eba1077263f206fd6284be8b0a7af3`: iOS build 21 (`[redacted]`) and Android version code 12 (`[redacted]`). That commit restores a way out of the event room, makes the camera one tap, trims the activation confirmation to channel reach, and connects the client failure reporter that 1.0.9 shipped without. Prior evidence: on 2026-09-01 EAS finished 1.0.9 from merged commit `b4fd752b1b3146e123e404201d937ba03abbf8ab`: iOS build 19 (`[redacted]`) and Android version code 11 (`[redacted]`). That commit unsticks the photo sheet, which in 1.0.8 stranded a selected photo with no control able to post it, and adds client failure reporting. Prior evidence: on 2026-09-01 EAS finished 1.0.8 from merged commit `b1d15a198055514d63a68dea2b7819a42555f1e3`: iOS build 18 (`[redacted]`) and Android version code 10 (`[redacted]`). That commit carries one-action photo posting, compact notification headlines, the end-event confirmation without rendered message bodies, and the event-summary PDF font fix. Prior evidence: on 2026-08-31 EAS finished 1.0.7 from merged commit `3008d65fdc2b021f6ba0abaf22f98ba674bc3602`: iOS build 17 (`[redacted]`) and Android version code 9 (`[redacted]`). That commit carries the phone event-room removal of correct and redact, and the fix for every end-event from the phone failing with "The event request is invalid" because the client sent a `confirmationPhrase` the route forbids. Prior evidence: on 2026-08-30 EAS finished 1.0.6 from merged commit `ed99e48c4b8ed6e280b137654d874f83c7aebef5`: iOS build 16 (`[redacted]`) and Android version code 8 (`[redacted]`). An earlier Android attempt (`[redacted]`, code 7) compiled for 951 seconds and then failed inside EAS with `SERVER_ERROR: Failed to upload application archive`; it produced no artifact and was rebuilt rather than retried in place. The prior 1.0.5 artifacts were iOS build 12 (`[redacted]`) and Android code 6 (`[redacted]`). This proves signed artifact production, not provider acceptance or installation.

### EAS Update

Status: `blocked`

Remote updates are disabled for the current embedded-only runtime. This is an enforced architectural boundary, not a configuration default: push registration derives `updateMode` from the disabled-updates constants in `packages/mobile/src/lib/push/native-port.ts`, `NativePushBuildIdentitySchema` accepts only the `embedded-only` literal, and the `device_push_token_registrations_embedded_only` CHECK constraint rejects any other value. Enabling remote updates without first changing the contract, adding a superseding migration, and reworking the update diagnostic would stop all push registration.

### EAS Submit

Status: `live-verified`

On 2026-09-25 exact iOS submission `91e6608d-bfb8-44b6-8035-2c19ce1d0ff0` uploaded 1.0.17/build 32 to App Store Connect (succeeded 07:21) and exact Android submission `120bb843-686c-4bc4-aa14-3ca31aeec9b7` uploaded 1.0.17/code 24 to Play (succeeded 07:42); both read back as Succeeded on their EAS submission pages. Prior evidence: On 2026-09-22 exact iOS submission `[redacted]` uploaded 1.0.16/build 31 to App Store Connect (started 07:46, succeeded 07:48) and exact Android submission `[redacted]` uploaded 1.0.16/code 23 to Play (started 07:55, succeeded 07:58). Both were read back as Succeeded on their EAS submission pages. Prior evidence: On 2026-09-08 exact iOS submission `[redacted]` uploaded 1.0.14/build 26 to App Store Connect (finished 19:28Z after 36 minutes in the EAS queue) and exact Android submission `[redacted]` uploaded 1.0.14/code 17 to the Play Internal track with `releaseStatus: completed` (finished 19:14Z). Both were reported finished by EAS. App Store Connect, read from this workstation at 2026-09-08 19:32Z, listed the 1.0.14 (26) upload as Complete (created Sep 8, 2026 12:28 PM Pacific) and the build as attached to the PSD EOC Internal TestFlight group with 3 invites and no installs yet. No Alpha Closed-testing draft was staged for 1.0.14. Prior evidence: on 2026-09-05 exact iOS build 25 was uploaded to App Store Connect and exact Android code 16 was uploaded to the Play Internal track with `releaseStatus: completed`; both were reported complete by EAS. Prior evidence: on 2026-09-01 exact iOS build 21 was uploaded to App Store Connect and exact Android code 12 was uploaded to the Play Internal track with `releaseStatus: completed`; both were reported complete by EAS. Apple-side processing completion was not observed from this workstation. No Alpha Closed-testing draft was staged for 1.0.10. Prior evidence: on 2026-09-01 exact iOS build 19 was uploaded to App Store Connect and exact Android code 11 was uploaded to the Play Internal track with `releaseStatus: completed`; both were reported complete by EAS. Apple-side processing completion was not observed from this workstation. No Alpha Closed-testing draft was staged for 1.0.9. Prior evidence: on 2026-09-01 exact iOS build 18 was uploaded to App Store Connect and exact Android code 10 was uploaded to the Play Internal track with `releaseStatus: completed`; both were reported complete by EAS. Apple-side processing completion was not observed from this workstation. No Alpha Closed-testing draft was staged for 1.0.8. Prior evidence: on 2026-08-31 exact iOS submission `[redacted]` uploaded 1.0.7/build 17 to App Store Connect and exact Android submission `[redacted]` uploaded 1.0.7/code 9 to the Play Internal track with `releaseStatus: completed`. Both were reported complete by EAS. Apple-side processing completion was not observed from this workstation: the App Store Connect API key is held on EAS servers, so no local readback exists. No Alpha Closed-testing draft was staged for 1.0.7. Prior evidence: on 2026-08-30 exact iOS submission `[redacted]` uploaded 1.0.6/build 16 to App Store Connect and exact Android submission `[redacted]` uploaded 1.0.6/code 8 to the Play Internal track with `releaseStatus: completed`. Both were reported complete by EAS. Apple-side processing completion was not observed from this workstation: the App Store Connect API key is held on EAS servers, so no local readback exists. The prior 1.0.5 submissions were `[redacted]` (build 12, processing completed) and `[redacted]` (code 6, Internal testing). This proves provider upload, not exact-device installation.

### TestFlight device installation

Status: `live-verified`

On 2026-09-25 App Store Connect showed 1.0.17/build 32 Complete. The same day 1.0.16 was removed from review after three days at Waiting for Review, the version was edited to 1.0.17 in place, build 31 was replaced by 32, and the version was resubmitted; App Store Connect read "1 Item Submitted" and 1.0.17 Waiting for Review, and Apple granted an expedited review ("We'll expedite review for PSD EOC"). The review notes direct reviewers to the app-store review sign-in and state the 2026-09-30 service cutoff. No installation of 1.0.17 on a physical device is retained yet. Prior evidence: On 2026-09-22 App Store Connect showed 1.0.16/build 31 with upload status Complete at 07:48. No installation of 1.0.16 on a physical device is retained yet. The same day 1.0.15/build 30 was removed from review, the version string was edited to 1.0.16 in place, build 30 was replaced by 31, and the version was resubmitted: App Store Connect reads "1 Item Submitted" and the sidebar shows 1.0.16 Waiting for Review, with Apple stating review can take up to 48 hours. Removing a version from review moves it to Developer Rejected first, which is the expected intermediate state and not an Apple refusal, and it forfeits the queue position. Prior evidence: On 2026-09-08 App Store Connect showed 1.0.14/build 26 (EAS build `[redacted]`, source `e288104675249295edec646102b1516bc45e49f1`) processed and attached to the PSD EOC Internal group with 3 invites; no installation of 1.0.14 on a physical device is retained yet. Prior evidence: 1.0.10/build 21 was uploaded on 2026-09-01. 1.0.9/build 19 was installed on a physical iPhone and reported usable for photo posting, which is the first retained confirmation of that flow, but it had no way out of the event room: an operator who ended an event had to force-quit the app, because that stack holds the room alone and a room opened from a notification is its own root with no back control. That is fixed in 1.0.10, whose installation is not yet retained. 1.0.8/build 18 was installed on a physical iPhone and reported unusable for photo posting: once a photo was chosen both capture buttons were disabled and no control could post it, so the app had to be force-quit. That is fixed in 1.0.9, whose installation is not yet retained. The boundary below is proven for 1.0.7/build 17. On 2026-08-31 the product owner confirmed running exact 1.0.7/build 17 on a physical iPhone and receiving a drill push notification on it; the device's 1.0.7 push registrations are retained in the database at 2026-08-31T15:32:56Z. Prior evidence: on 2026-08-30 the product owner confirmed installing exact 1.0.6/build 16 on a physical iPhone through the existing internal tester group and reported the event room rendering. Internal distribution requires no Beta App Review, so no review state gated it. On that install, push registration was refused by the server, which is what surfaced the build allowlist described below; the allowlist has since been removed and deployed, and a successful registration on 1.0.6 is not yet retained. Prior evidence: on 2026-08-28 the internal tester group was created with automatic distribution enabled and one App Store Connect user holding the Developer role as its sole tester, all four retained builds including 1.0.5/build 12 showed `Ready to Test`, and build 12 was installed on a physical iPhone 15 Pro running iOS 26.6. This proves exact-build physical installation. No in-app Release diagnostic readback or notification observation is retained. On 2026-09-02 App Store Connect showed the first two invited internal testers active: both accepted their invitations and were added to the "PSD EOC Internal" group that day; one install was observed on a current iPhone running iOS 26, and the other showed "No Builds Available" at the moment of adding, which is the state before a newly added internal tester opens TestFlight.

### Google Play device installation

Status: `configured-unverified`

On 2026-09-25 exact 1.0.17/code 24 was uploaded from EAS build `273bc4c9-4705-46ae-86d6-d19f1bedb4a7` and read back in the Play bundle library. No physical installation of it is retained. Prior evidence: On 2026-09-22 exact 1.0.16/code 23 was uploaded from EAS build `[redacted]` and read back in the Play Console bundle library. No Play-side installation on a physical device is retained for it. Prior evidence: On 2026-09-08 exact 1.0.14/code 17 was submitted to the Internal track from EAS build `[redacted]` built from source `e288104675249295edec646102b1516bc45e49f1`; the submission was reported finished by EAS. No Play-side readback or physical installation is retained for it. Prior evidence: on 2026-09-01 exact 1.0.10/code 12 was uploaded to the Internal track with `releaseStatus: completed`; no Play-side readback or physical installation is retained for it. Prior evidence: on 2026-09-01 exact 1.0.9/code 11 was uploaded to the Internal track with `releaseStatus: completed`; no Play-side readback or physical installation is retained for it. Prior evidence: on 2026-09-01 exact 1.0.8/code 10 was uploaded to the Internal track with `releaseStatus: completed` and EAS reported the submission complete; no Play-side readback or physical installation is retained for it. Prior evidence: on 2026-08-31 exact 1.0.7/code 9 was uploaded to the Internal track with `releaseStatus: completed` and EAS reported the submission complete; Play-side activation was not read back and no exact-build physical installation, launch, or diagnostic readback is retained for it. Prior evidence: on 2026-08-30 exact 1.0.6/code 8 was uploaded to the Internal track with `releaseStatus: completed` and EAS reported the submission complete. Play-side activation was not read back from this workstation, and no exact-build physical installation, launch, or diagnostic readback is retained. Prior evidence: on 2026-08-26 Play showed 1.0.5/code 6 active and available to the existing `PSD EOC initial pilot` Internal testers, code 5 was deactivated by the replacement, and the same code 6 artifact, tester list, and United States targeting were saved in the Alpha Closed-testing draft without rollout. On 2026-09-05 exact 1.0.13/code 16 was submitted to the Internal track from EAS build `[redacted]` built from source `18ce5eef40ee36c587ebac709301c867309d235c`; the submission was reported accepted by EAS. No Play-side readback, physical installation, or push arrival is retained for it. 1.0.13 is the first build in which an Android device can register for push at all: earlier builds gated permission on an Android channel lock-screen visibility the operating system does not honour from an ordinary application, so every Android device reported denied, requested no token, and was actively unregistered.

### Public mobile privacy policy

Status: `live-verified`

On 2026-08-26 production deploy run `32975859074` completed from merged commit `22592929`; an anonymous request to the configured production origin's `/privacy` path returned HTTP 200 without a redirect or `Set-Cookie`, and Play saved that exact configured URL as the app privacy policy. The page publishes the configured public-records contact and the staff-data/provider disclosures used for the store declaration. This does not prove the link from an exact-build physical-device installation.

### Play app content and store record

Status: `configured-unverified`

On 2026-09-25 Google refused 1.0.16/code 23 with "Login credentials restricted by device authentication", asking for "a dedicated test bypass or standard login credentials that do not require your account to be linked to our testing devices". The evidence was again this app's sign-in screen reading "Google sign-in was cancelled": the reviewer never got back from Google and district single sign-on. Production release 24 (1.0.17) replaces 23, the reviewer instructions now direct reviewers to the app-store review sign-in, the review account's password field holds the review code, and all 13 staged changes were sent for review the same day; the Publishing overview reads "Changes in review". Before submitting, the review sign-in was exercised on production with the real review code: a wrong code was refused with 401, the real code signed in as a staff session limited to the review facility, and that session was revoked. Android developer verification was confirmed complete for all Play apps the same day. Prior evidence: On 2026-09-22 the production track carries release 23 (1.0.16) and all 13
staged changes were sent for review; the Publishing overview reads "Changes in
review". That release replaces 22 (1.0.15), which Google refused on 2026-09-22
under Play Console Requirements with "Login credentials are incorrect". The
credentials were not the cause. Google's evidence screenshot is this app's own
sign-in screen showing "Sign-in did not start / Google sign-in was cancelled.
No device session was created.", which is the Android redirect race described
under EAS Build; no device could have completed sign-in on code 22 whatever
credentials were supplied. The same race is the likeliest cause of the
2026-09-18 refusal of code 21 for Broken Functionality. Separately, the Play
reviewer sign-in instructions were themselves wrong and were corrected the same
day: they read "enter the USERNAME above (NOT the email)" while the username
field holds a full address, so a reviewer following them literally would have
entered the full address at the ClassLink page, which refuses it. The saved
text now names the local part explicitly and is 489 of the 500 characters Play
allows. App Store Connect's review notes already stated this correctly and were
not changed. Google's confirmation dialog states reviews are typically
completed within 7 days. Prior evidence: on 2026-08-26 Play showed 8/11 setup
tasks complete, with reviewer sign-in details, target audience, final Data
Safety submission, and Advertising ID still needing attention, and no Closed
rollout or store-listing review submitted.

### Expo Push notification handoff

Status: `live-verified`

On 2026-08-27 direct CDK deployed source `7a0164838008667e8b138197ba749c376ebaf39b` and matching application/bootstrap digest `sha256:[redacted]`. The protected credential is `verified`, the exact verification reference is deployed, and the Expo worker is active with a completed rollout at desired/running/pending 1/1/0; continuing heartbeats reported zero receipts and zero stuck outbox work. Provider-free iOS 26.5 and Android 16 native simulator drills passed local operating-system presentation, tap routing to the exact DRILL event, text update, and all-clear with registration forced off. On 2026-08-30 direct CDK deployed merged source `2fdb065`, which removed the per-build push registration allowlist; the live App Runner configuration no longer carries `PSD_EOC_PUSH_REGISTRATION_BUILD_ALLOWLIST` and `/api/health` returned HTTP 200 on the new image. That allowlist gated both registration and send eligibility on an exact hand-maintained build tuple, so any release absent from it lost push silently; its Secrets Manager value is scheduled for deletion on 2026-09-06. On 2026-08-30 the product owner registered push on 1.0.6 and reported that a drill sent notifications after republishing the roster snapshot, which is the first retained end-to-end push observation and the first successful 1.0.6 registration. Republishing was required because a roster snapshot froze a copy of each device push registration and matched it back by `registration.id = endpoint.id`; reinstalling the app enrolls a new installation and mints a new registration id and token, so the frozen endpoint matched nothing, zero push endpoints resolved, and nothing was enqueued. Email was unaffected because an address is stable. On 2026-09-01 a further direct `cdk deploy` of merged source `718eccda89eba1077263f206fd6284be8b0a7af3` reached `UPDATE_COMPLETE`; the stack reports that SHA and the configured production origin's `/api/health` returned HTTP 200. Earlier that day a `cdk deploy` of merged source `b1d15a198055514d63a68dea2b7819a42555f1e3` reached `UPDATE_COMPLETE`; the stack reports that SHA as `DeploymentSourceSha` and the configured production origin's `/api/health` returned HTTP 200. Merged commit `86bdc1e` resolves staff push endpoints live from recipient identity and substitutes the live token into the audience the push runtime resolves against. On 2026-08-31 a direct `cdk deploy` of merged source `b68f07fe1c74ac558ffb4e4455d1ed05f245c1b5` reached `UPDATE_COMPLETE` in 491 seconds; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-08-31T06:42:31-07:00, the configured production origin's `/api/health` returned HTTP 200, and the Expo push worker is `ACTIVE` at desired/running/pending 1/1/0 with rollout `COMPLETED`. On 2026-08-31 the product owner ran a drill on 1.0.7/build 17 and confirmed the push notification arrived on a physical iPhone, which is the first retained observation of a push send through the deployed live resolution. A read-only one-off bootstrap task confirms what that proves: the current staff roster snapshot `aa3e94e6` (captured 2026-08-31T05:39:24Z) carries exactly one push endpoint, bound to 1.0.6/build 16 registration `9167f310`, and that registration now carries an unregistration record. The live 1.0.7/build 17 registrations were made at 2026-08-31T15:32:56Z, hold different identifiers, and appear in no snapshot. Under the previous id-bound resolution that endpoint resolved to an unregistered registration and was reported `disabled`, so the send was only possible through live resolution. A reinstalled or updated device no longer needs a roster republish to be reachable, and this is now observed rather than inferred. Expo tickets and final receipts are both retained. Each accepted send records an `expo-push` ticket as its `provider-accepted` delivery evidence and schedules a receipt poll fifteen minutes later; a read-only bootstrap probe on 2026-08-31 shows eight retained polls, each claimed after its due time and carrying a terminal decision of `{kind: complete, state: provider-accepted}` at `poll_attempt_number` 1 with `receipt_reference_state` `unique` and no lease outstanding. The drill attempts are among them: `88458521` was sent at 15:33:48Z, its poll fell due at 15:48:48Z, and Expo's receipt was decided at 15:49:02Z; `717b5461` was sent at 15:35:21Z, fell due at 15:50:21Z, and was decided at 15:51:03Z. Each decision lands after its own due time rather than at scheduling time, so these are provider round trips rather than short-circuited schedules. An Expo receipt without an error means Expo accepted the message and handed it to the platform push service; Expo exposes no device-level delivery signal, so device arrival is evidenced separately by the product owner's observation and not by this record. On 2026-09-01 the product owner installed the app on a second device and reported push arriving only there and no longer on the phone. A read-only bootstrap probe confirmed the cause: two live device enrollments, each holding a live APNs and a live Expo 1.0.10/build 21 registration, while staff snapshot `aa3e94e6` (v4) held a single push endpoint bound to the unregistered 1.0.6 registration `9167f310`; live resolution ranked registrations newest-first and paired by position, so the one endpoint took the newest device and the other went silent. Merged `#426` keeps an endpoint on its own live registration while it exists, fans out one endpoint per additional live device on the provider profile the snapshot already uses, reads registrations as of the batch's creation instant so every page of a batch computes the same devices, and answers every policy candidate (failing closed to `disabled`) so one device unregistering cannot abort a send. Merged `#427` selects every others source on every activation and adds a manual others source, curated in the application without a Google Group, across the contracts, the handler, and the `group_sources_valid_variant` CHECK through migration `0043_manual_others_source`. Later that day a direct `cdk deploy` of merged source `1cacb430c9732dc84bf97d7a2cc5441724424d0d` reached `UPDATE_COMPLETE` at 20:23:42Z after a 413-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the configured production origin's `/api/health` returned HTTP 200, and the Expo push worker is `ACTIVE` on task definition revision 44 at desired/running/pending 1/1/0 with rollout `COMPLETED`. A read-only bootstrap probe after the deploy read `drizzle.__drizzle_migrations` with its newest row at `created_at` 1787939022446, the `0043` journal entry, and read the live `group_sources_valid_variant` definition carrying the manual/others predicate, so the three layers that encode the rule agree in production. Arrival on both of one person's devices is not yet observed; the product owner's next two-device drill is that observation, and this record will say so when it has happened. On 2026-09-02 a direct `cdk deploy` of merged source `8143a6dc080fb1e271cdf2e238c589e913a9bdfb` reached `UPDATE_COMPLETE` after a 503-second deployment; the stack reports that SHA as `DeploymentSourceSha`, the App Runner `UPDATE_SERVICE` operation succeeded at 2026-09-01T19:32:11-07:00, the configured production origin's `/api/health` returned HTTP 200, and the Expo push worker is `ACTIVE` on task definition revision 45 at desired/running/pending 1/1/0 with rollout `COMPLETED`. That source carries merged `#429`: saving the people on a manual roster source now publishes the roster in the same request, or reports that the people were saved and why the roster did not publish, so no separate publish step remains for a curated list. On 2026-09-05 direct CDK deployed source `2ed2ecae4dea061d75c82861292b67c36ccd28e8` to `PsdEoc`; `DeploymentSourceSha` and an unauthenticated `/api/health` 200 were read back, and the push, email, and email-callback worker services each reported 1/1/0. That deploy carries two corrections. Audience resolution no longer requires a recipient to already hold a published push endpoint of a matching `platform

### Direct APNs notification handoff

Status: `mocked`

HTTP/2 request, ES256 credential, response-classification, dual-registration, provider-routing, protected-secret, and dark-cutover boundaries are covered by provider-free tests. No APNs credential rotation, provider acceptance, physical-device presentation, tap, or human observation is retained.

### Direct FCM notification handoff

Status: `mocked`

HTTP v1 request, OAuth credential, response-classification, dual-registration, provider-routing, protected-secret, and dark-cutover boundaries are covered by provider-free tests. No FCM credential rotation, provider acceptance, physical-device presentation, tap, or human observation is retained.

### Amazon SES notification handoff

Status: `live-verified`

On 2026-09-23 a read-only review of the production email workers covered the 25 days back to 2026-08-29. The log group `/psd-eoc/workers/email` recorded 31 `email-worker-message-completed` and 29 `email-worker-message-suppressed` events and no retirements. `/psd-eoc/workers/email-callback` recorded 284 `email-callback-message-completed` events: these are SES delivery and bounce notifications consumed from the encrypted SNS topic and recorded against the delivery ledger. The `psd-eoc-email-dlq` queue held 0 messages. Production dispatch rows for the two App Review incidents of 2026-09-16 (events `[redacted]` and `[redacted]`) each show one email attempt per activation and per all-clear. PR #481 fixed the two email defects the 2026-09-08 drills found: all-clear emails were always suppressed, and one invalid address could suppress every address. PR #470 removed the verification reference and the truth-label gate described below, so the channel is now gated only by its enabled flag and the `EnableEmailWorker` stack parameter. This entry proves SES acceptance and SES-reported delivery. It does not prove that each recipient read the message.

Prior evidence: On 2026-08-28T02:16:53Z read-only inventory showed account `ProductionAccessEnabled: true` and `SendingEnabled: true`, the configured sender domain verified with DKIM `SUCCESS`, suppression active for BOUNCE and COMPLAINT, and the retained configuration set with `SendingOptions.SendingEnabled: true`. This supersedes the earlier claim that the configuration set remained sending-disabled; the channel was dark because of deployment parameters, not SES. Direct `cdk deploy` of merged commit `201e260737f91e24824bed7c251adf87b5cf298b` then set `EnableEmailWorker=true` and `SesCredentialVerificationReference=issue-277-ses-credentials-2026-08-28T021653Z`, reaching `UPDATE_COMPLETE`. The email worker is `ACTIVE` at desired/running/pending 1/1/0 with continuing heartbeats and no errors; the stack reports `SesIntegrationTruth = configured-awaiting-human-verification`. The retained evidence record is held in the district's private operations records. No administrator verification action, provider send, delivery-ledger row, or human receipt exists.

### AWS End User Messaging SMS

Status: `live-verified`

On 2026-09-16 the store reviewer ran two real incidents at the isolated App Review facility (events `[redacted]` and `[redacted]`). All four SMS batches from those incidents, two activations and two all-clears, were refused before any attempt existed. Each batch correctly held zero endpoints, because the facility's audience had no SMS number, but the SMS runtime store recounted every active SMS endpoint in the district snapshot (one) and answered `BATCH_CONFLICT`. The worker retired each batch to `psd-eoc-sms-dlq` on its first HTTP 409, which put `psd-eoc-sms-dlq-depth` in `ALARM` from 17:11Z. No number was owed a text, so no delivery was lost. The same recount would refuse every text at a school as soon as an opted-in number outside that school's audience existed. On 2026-09-23 the district snapshot held that single opted-in number, which every non-isolated audience includes. PR #524 makes the store count the facility audience. The dead-letter queue also holds one `sms-opt-out-reconciliation` tick, retired at 2026-09-19T17:21Z while the App Runner container restarted after exit code 139. That tick recurs on a schedule and carries no recipient work.

On 2026-09-08 a staff drill activation (event `[redacted]` at a district office facility, started from the web at 20:32:42Z) reached one opted-in staff number end to end: the SMS worker (ECS 1/1 on merged commit `ebf5bca`) called `SendTextMessage` from the toll-free pool `pool-<redacted>` with configuration set `psd-eoc-sms` and protect configuration `protect-<redacted>`; AWS accepted it as MessageId `<provider-message-id>` (attempt `[redacted]`, evidence `provider-accepted` at 20:32:55Z); the carrier reported `TEXT_DELIVERED` through the EventBridge rule `psd-eoc-sms-delivery-events` into `psd-eoc-sms-receipts`; and the recipient confirmed the text on the handset; the attempt now carries canonical evidence `attempted` (20:32:54Z), `provider-accepted` (20:32:55Z) and `delivered` (recorded 22:24:37Z). The delivered receipt was recorded against the attempt only after merged commit `1799109` (PR #481, deployed 22:20Z) taught the worker that AWS publishes these events without the send's `Context`, so correlation keys on the retained MessageId; the four receipts dead-lettered before that deploy were redriven at 22:23Z and the worker recorded the two for this send and ignored the two for AWS's own verification text after two deferrals, leaving every SMS queue empty. Toll-free registration `registration-<redacted>` is COMPLETE. On 2026-09-09 AWS granted production access: the support request, refiled at AWS's direction as a service limit increase plus a Service Quotas request for `TextMessageMonthlySpend` (case ids are held outside current documentation because they carry the account number), was approved at 14:56Z; `describe-account-attributes` then read `ACCOUNT_TIER = PRODUCTION` and `describe-spend-limits` read a text-message monthly maximum of USD 100 with the enforced limit still USD 1, so `set-text-message-spend-limit-override --monthly-limit 100` was applied at 14:57Z and read back as enforced USD 100, overridden. Any staff number that opts in at `/text-alerts` can now receive; the sandbox's verified-destination restriction no longer applies. Earlier the same day, in order, a drill was refused before provider I/O by the adapter's own copy of the classification frame (`AWS_EUM_WORK_ITEM_INVALID`, PR #477), by IAM because only the pool was granted (`AWS_EUM_ACCESS_DENIED`, PR #478), and by the sandbox for an unverified destination (`AWS_EUM_CONFLICT`, twice). PR #481 also made all-clear texts and emails send when an operator ends an event, and stopped one invalid email address from suppressing every address. Prior evidence: Registration tooling, a conditional retained provider topology, fail-closed worker composition, immutable provider/retry state, and AWS-managed STOP reconciliation are source-defined and provider-free tested. No approved origination identity, deployed live worker, controlled handset send, carrier delivery, or human receipt is retained. Registration alone cannot establish readiness.

### Event media and record exports

Status: `blocked`

Synthetic object metadata and authorization behavior are covered; no live storage operation is claimed. Separately, the event-summary PDF export failed in production for every attempt: on 2026-09-01 retained logs show `export-event-summary` failing `PERSISTENCE_CONFLICT` with `database refused: code ENOENT`. ENOENT is not a SQLSTATE -- the engine's refusal classifier matched any error carrying a string `code`, so a missing file reported itself as a database fault. The missing file was the renderer's Noto Sans asset: Next traces it as a relative dependency of each route chunk but emits it once into the content-hashed media directory, so the traced path did not exist. Merged commit `b1d15a198055514d63a68dea2b7819a42555f1e3` adds a bounded fallback to the emitted asset, still verified against `FONT_SHA256`, and narrows the refusal classifier to real five-character SQLSTATEs. Deploying it fixed the font and moved the failure to the next untraced asset: on 2026-09-01 a retained log records `ENOENT: no such file or directory, open '/app/packages/server/.next/server/chunks/data/Helvetica.afm'` -- PDFKit's own metrics for a default font this renderer never uses, since every write selects the embedded Noto Sans by name. That the log names the real file rather than reporting a database refusal is the narrowed classifier working. Merged commit `b4fd752b1b3146e123e404201d937ba03abbf8ab` requests no default font at all and is deployed. It was verified by making PDFKit's `data` directory unavailable, which reproduces the deployed condition exactly: without the change the render fails with that identical ENOENT, and with it the render succeeds. No successful production export is retained, so this stays unproven. Neither failure is reachable by a unit test: in a test run both the font asset and `node_modules` resolve, and only the bundled output lacks them, so this class of defect needs the built artifact exercised. The rendering tests additionally require Poppler and skip when `pdftotext` is absent from PATH, which it is inside `bun run check`. No build-output check exists yet.

### GuardDuty Malware Protection for S3

Status: `configured-unverified`

CDK defines the scoped plan and fail-closed tag boundary; no active-plan readback or controlled scan is retained.

### OpenStreetMap raster tiles

Status: `configured-unverified`

The optional read-only configuration is source-defined; no live availability claim is retained.

## Records-retention review

<!-- psd-eoc:records-retention-review-status:start -->

The following official sources were rechecked on 2026-08-26:

- [Local Government Common Records Retention Schedule (CORE)](https://www.sos.wa.gov/sites/default/files/2025-06/local-government-common-records-retention-schedule-CORE.PDF):
  version 5.0, approved and effective October 2, 2024.
- [Public Schools (K-12) Records Retention Schedule](https://www.sos.wa.gov/sites/default/files/2026-06/Public-Schools-%28K-12%29-Records-Retention-Schedule.PDF):
  version 9.1, approved and effective June 3, 2026.
- [Washington State Archives school and ESD schedule index](https://www.sos.wa.gov/archives/help-government-agencies/managing-school-and-esd-records):
  identifies those two versions as the current schedules used together.

CORE v5.1 and K-12 v9.2 are non-authoritative draft revisions. They are future
change inputs only until the Local Records Committee approves them and the
official index replaces the current versions. Do not commit schedule PDFs to
the repository.

These current schedule entries are candidates for records-officer review, not
repository-owned classifications:

<!-- docs-contract:records-retention-candidates:start -->

| Content and context                                                                                 | Candidate series    | Current minimum/action                                                                                                                                               | Source designation |
| --------------------------------------------------------------------------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| Emergency-drill or occupational-safety-program administration                                       | `GS2017-016 Rev. 0` | Retain for 3 years after the end of the calendar year and until no longer needed for agency business, then destroy; non-archival.                                    | OPR                |
| Routine/minor emergency response with minimal assistance or disruption                              | `GS2012-025 Rev. 1` | Retain for 6 years after the matter is resolved or recovery is complete, then destroy; non-archival.                                                                 | OPR                |
| Uncommon/major response, including emergency-operations-center activities and communications        | `GS50-18-29 Rev. 2` | Retain for 6 years after the matter is resolved or recovery is complete, then transfer to Washington State Archives for appraisal and selective retention; archival. | OPR                |
| Security incidents or data/privacy breaches, including investigation and notification documentation | `GS2010-008 Rev. 2` | Retain for 6 years after the matter is resolved, then destroy; non-archival.                                                                                         | OFM                |
| School safety plans, only if the product actually owns that content                                 | `SD2011-153 Rev. 1` | Retain for 6 years after obsolete or superseded, then destroy; non-archival.                                                                                         | OPR                |

<!-- docs-contract:records-retention-candidates:end -->

- Official sources last rechecked: `2026-08-26`.
- Controlled mapping review status: `pending`.
- Controlled mapping review date: `not completed`.
- Controlled mapping inventory coverage: `not completed`.
- Controlled mapping ambiguity status: `not completed`.

The tenant-neutral inventory and durable boundaries are recorded in
[Architecture and contributing](ARCHITECTURE.md#records-retention-classification);
the current source versions and aggregate evidence are retained in this
section. The tenant's row-level DAN decisions and private evidence belong only
in its controlled operations record. No completed records-officer review or
ambiguity guidance has been supplied for this aggregate status. Until it is,
retain every product record and do not enable disposition automation.

<!-- psd-eoc:records-retention-review-status:end -->

Dated observations recorded before the 2026-08-25 consolidation and the
append-only deployment ledger are retained in the district's private
operations records, outside this repository.

Advancing a provider entry requires evidence for exactly the claimed boundary.
Provider acceptance is not delivery or human receipt. A live notification test
also requires verified credentials, an approved synthetic target, and an
authenticated human acting in the application; automation cannot send it.
